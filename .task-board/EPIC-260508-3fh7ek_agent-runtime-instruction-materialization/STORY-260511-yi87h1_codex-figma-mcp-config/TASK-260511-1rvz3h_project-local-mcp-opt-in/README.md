# TASK-260511-1rvz3h: project-local-mcp-opt-in

## Description
Remove MCP servers from global Codex defaults. Add project-local agents-infra config support for an explicit enabled MCP list and install a local Codex launcher that passes only opted-in MCP servers via Codex -c overrides.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
