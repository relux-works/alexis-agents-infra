# BUG-260615-9u5jvq: mcp-starts-without-project-config

## Description
Investigate report that opening a project without .agents/.configs/project-config.toml still prints MCP startup attempts. Verify Codex config rendering and MCP server launch behavior, then fix if a regression exists.

## Scope
(define bug scope / affected area)

## Acceptance Criteria
(define fix acceptance criteria)
