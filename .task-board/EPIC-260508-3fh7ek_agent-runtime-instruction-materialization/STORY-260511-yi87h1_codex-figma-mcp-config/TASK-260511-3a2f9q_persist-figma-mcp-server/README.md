# TASK-260511-3a2f9q: persist-figma-mcp-server

## Description
Add the official remote Figma MCP endpoint to source-managed Codex config, sync global runtime config, verify codex mcp list/get, and document that existing sessions must restart before Figma MCP tools are mounted.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
