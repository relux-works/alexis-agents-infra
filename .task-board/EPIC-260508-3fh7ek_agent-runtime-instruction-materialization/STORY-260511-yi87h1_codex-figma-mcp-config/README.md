# STORY-260511-yi87h1: codex-figma-mcp-config

## Description
Make Codex MCP servers project-local opt-in instead of global defaults. Keep global Codex config free of MCP servers, maintain a source-managed registry of known MCP endpoints, and install local launchers only for projects that explicitly enable them.

## Scope
Global agents-infra source config, local setup flow, doctor reporting, README docs, and Tap2Cash app local runtime setup.

## Acceptance Criteria
(define acceptance criteria)
