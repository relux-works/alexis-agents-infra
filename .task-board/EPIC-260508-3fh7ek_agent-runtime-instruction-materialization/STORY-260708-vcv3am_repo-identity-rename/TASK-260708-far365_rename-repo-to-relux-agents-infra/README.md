# TASK-260708-far365: rename-repo-to-relux-agents-infra

## Description
Rename the repository from relux-agents-infra to relux-agents-infra. Update source-controlled names, module/import paths, docs, generated instruction references, setup metadata, and remote URLs so the checked-out project no longer contains personal identity references. Rename the GitHub repository and the local checkout folder. Verify with repository-wide search excluding .git and transient .temp artifacts.

## Scope
(define task scope)

## Acceptance Criteria
(define acceptance criteria)
