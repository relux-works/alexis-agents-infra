Root cause: agents-infra codex scanned ancestors all the way through $HOME and treated ~/.agents/.configs/project-config.toml as project-local opt-in, enabling Figma for projects without local MCP config. Fix: skip the home-level ~/.agents project-config in BuildCodexLaunchPlan, remove stale global project-config during setup global, and install agents-infra binary atomically via temp file plus mv. Verified no-config project has enabled_mcp=(none), per-project opt-in still enables figma, codex mcp list remains empty, go test ./... passes, and ./setup.sh verifies global setup.