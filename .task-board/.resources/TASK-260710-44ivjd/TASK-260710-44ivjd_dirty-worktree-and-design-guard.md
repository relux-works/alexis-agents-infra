User requirement: image parsing guidance/tooling belongs in relux-agents-infra and must be completely board-agnostic because agents-infra cannot assume a task board exists. Preserve all pre-existing dirty changes. At task creation the modified paths included .instructions/AGENTS.md, .instructions/INSTRUCTIONS.md, .instructions/INSTRUCTIONS_WORKFLOW.md, README.md, tools/agents-infra/internal/infra/infra_test.go, board progress, and new remote-agent instruction files. Inspect git diff before each overlapping edit; never reset, checkout, overwrite whole files, stage, or commit. The physical-SIM lesson is: originals read-only; normalize HEIC to scratch; direct visual inspection first; OCR only as a bounded fallback; tie every claim to a filename/confidence; redact identifiers; no task-board vocabulary or dependency in reusable artifacts.