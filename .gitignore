# External skills (not tracked — local repos, symlinks, machine-specific)
skills/

# Generated local agent runtime (installed/symlinked by agents-infra)
.agents/
.claude/
.codex/
.local/

# macOS
.DS_Store

# Local runtime scratch and backup artifacts
.temp/
*.bak.*

# Raw agent transcripts and command logs (task outcome summaries stay versioned)
.task-board/.resources/**/*.log
