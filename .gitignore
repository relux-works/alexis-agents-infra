# External skills (not tracked — local repos, symlinks, machine-specific)
skills/

# macOS
.DS_Store

# Local runtime scratch and backup artifacts
.temp/
*.bak.*

# Raw agent spawn transcripts (task outcome evidence stays local, not in history)
.task-board/.resources/**/*spawn-log*.log
